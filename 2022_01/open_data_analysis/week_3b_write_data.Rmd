---
title: "week_3b_write_data"
author: "junghyun"
date: '2022 4 19 '
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
```


```{r}
heights <- read_csv("data/heights.csv")

write_csv(heights,"data/heights_saved.csv")
```

1. `write_rds()` and `read_rds()`

```{r}
write_rds(heights, "data/heights.rds")
read_rds("data/heights.rds")
```
feather 라는 패키지를 사용하여 불러오면 R과 Python 모두에서
사용할 수 있다.
# rds 보다 빠름

```{r}
library(feather)

write_feather(heights, "data/heights.feather")
read_feather("data/heights.feather")
```
```{r}
# 파일제거
file.remove("data/heights_saved.csv")
file.remove("data/heights.rds")
file.remove("data/heights.feather")
```

